# ─── Backend API URL ──────────────────────────────────────────────────────────
# The Node.js Fastify backend runs on port 4000.
# All frontend pages use NEXT_PUBLIC_API_URL via lib/api-client.ts
NEXT_PUBLIC_API_URL=http://localhost:4000
